;;; theme.el --- RangHo's Emacs theme

;;; Code:

(deftheme rangho "RangHo's day-to-day Emacs theme")

;; Disable unnecessary UI elements
(menu-bar-mode -1)
(scroll-bar-mode -1)
(tool-bar-mode -1)

(provide-theme 'rangho)
